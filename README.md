There are 2 ways to run the program
1. Running gui.py directly on your ide, which generates a map user interface that allows users to input a start and end mrt station
2. go to the root of the folder in terminal and run python -m app.main